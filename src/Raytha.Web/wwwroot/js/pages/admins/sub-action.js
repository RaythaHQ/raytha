/**
 * Admins - Sub Action Layout
 * Handles admin user sub-actions (suspend, delete, etc.)
 */

import { ready } from '/js/core/events.js';

function init() {
  // Sub-action modals are handled by confirm-dialog.js
  // which auto-initializes all modals with forms
  
  // Add any admin-specific sub-action logic here
}

ready(init);

