{% comment %}
WYSIWYG Widget - Rich text content block.

Settings:
  - content: HTML content from WYSIWYG editor
  - backgroundColor: Background color (optional)
  - padding: Padding size - none, small, medium, large
{% endcomment %}

{% assign padding_class = 'py-4' %}
{% case widget.settings.padding %}
  {% when 'none' %}
    {% assign padding_class = '' %}
  {% when 'small' %}
    {% assign padding_class = 'py-2' %}
  {% when 'large' %}
    {% assign padding_class = 'py-5' %}
{% endcase %}

<div class="wysiwyg-widget {{ padding_class }}"
     {% if widget.settings.backgroundColor %}style="background-color: {{ widget.settings.backgroundColor }};"{% endif %}>
  <div class="wysiwyg-content">
    {{ widget.settings.content }}
  </div>
</div>

