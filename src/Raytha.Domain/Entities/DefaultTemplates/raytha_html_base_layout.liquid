<!DOCTYPE html>
<html lang="en" data-bs-theme="light">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    {% if Target.PrimaryField %}
    {{ Target.PrimaryField }} - {{ ContentType.LabelPlural }} | {{ CurrentOrganization.OrganizationName }}
    {% elsif ContentType.LabelPlural %}
    {{ ContentType.LabelPlural }} | {{ CurrentOrganization.OrganizationName }}
    {% else %}
    {{ CurrentOrganization.OrganizationName }}
    {% endif %}
  </title>

  <link rel="stylesheet" href={{ "bootstrap.min.css" | attachment_public_url }}>
  <link rel="shortcut icon" href={{ "favicon.ico" | attachment_public_url }} type="image/x-icon" />
  <link rel="shortcut icon" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/icons/box.svg" type="image/svg+xml" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>

<body class="d-flex flex-column min-vh-100 bg-body-tertiary">
  <header class="sticky-top">
    <nav class="navbar navbar-expand-lg bg-white shadow-sm">
      <div class="container">
        <a class="navbar-brand fw-bold text-primary d-flex align-items-center gap-2" href="{{ PathBase }}/">
          <i class="bi bi-box-fill"></i>
          {{ CurrentOrganization.OrganizationName }}
        </a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          {% assign menu = get_main_menu() %}
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0 gap-1">
            {% for menuItem in menu.MenuItems %}
              {% assign menuLabelDownCase = menuItem.Label | downcase %}
              <li class="nav-item">
                <a class="nav-link px-3 rounded-pill {% if Target.RoutePath == menuItem.Label or ContentType.DeveloperName == menuLabelDownCase %}active bg-primary bg-opacity-10 text-primary fw-semibold{% endif %}" href="{{ menuItem.Url }}">
                  {{ menuItem.Label }}
                </a>
              </li>
            {% endfor %}
          </ul>
          <div class="d-flex align-items-center ms-lg-3 gap-2">
            {% if CurrentUser.IsAuthenticated %}
            <div class="dropdown">
              <button class="btn btn-outline-secondary dropdown-toggle d-flex align-items-center gap-2" type="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi bi-person-circle"></i>
                {{ CurrentUser.FullName }}
              </button>
              <ul class="dropdown-menu dropdown-menu-end shadow border-0" aria-labelledby="userDropdown">
                <li><a class="dropdown-item d-flex align-items-center gap-2" href="{{ PathBase }}/account/me"><i class="bi bi-person"></i> My profile</a></li>
                <li><a class="dropdown-item d-flex align-items-center gap-2" href="{{ PathBase }}/account/me/change-password"><i class="bi bi-key"></i> Change password</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item d-flex align-items-center gap-2 text-danger" href="{{ PathBase }}/account/logout"><i class="bi bi-box-arrow-right"></i> Log out</a></li>
                {% if CurrentUser.IsAdmin %}
                <li><hr class="dropdown-divider"></li>
                <li><a href="{{ PathBase }}/raytha" class="dropdown-item d-flex align-items-center gap-2"><i class="bi bi-gear"></i> Admin panel</a></li>
                {% endif %}
              </ul>
            </div>
            {% else %}
            <a href="{{ PathBase }}/account/login" class="btn btn-outline-primary">
              <i class="bi bi-box-arrow-in-right me-1"></i>Login
            </a>
            <a href="{{ PathBase }}/account/create" class="btn btn-primary">
              <i class="bi bi-person-plus me-1"></i>Sign up
            </a>
            {% endif %}
          </div>
        </div>
      </div>
    </nav>
  </header>

  <main class="flex-grow-1">
    {% renderbody %}
  </main>

  <footer class="bg-dark text-white mt-auto">
    <div class="container py-4">
      <div class="row align-items-center">
        <div class="col-md-4 text-center text-md-start mb-3 mb-md-0">
          <span class="d-flex align-items-center justify-content-center justify-content-md-start gap-2">
            <i class="bi bi-box-fill text-primary"></i>
            <span class="fw-semibold">{{ CurrentOrganization.OrganizationName }}</span>
          </span>
        </div>
        <div class="col-md-4 text-center mb-3 mb-md-0">
          <small class="text-white-50">&copy; 2026 {{ CurrentOrganization.OrganizationName }}. All rights reserved.</small>
        </div>
        <div class="col-md-4 text-center text-md-end">
          <a rel="nofollow" href="https://raytha.com" class="text-white-50 text-decoration-none d-inline-flex align-items-center gap-1">
            <span>Powered by</span>
            <span class="fw-semibold text-white">Raytha CMS</span>
            <i class="bi bi-arrow-up-right-square"></i>
          </a>
        </div>
      </div>
    </div>
  </footer>

  <script src={{ "bootstrap.bundle.min.js" | attachment_public_url }}></script>
</body>

</html>
